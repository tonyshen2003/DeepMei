# DeepMei 废弃代码与死文件扫描报告

扫描范围：整个项目（`/Users/shensunfeng/DeepMei`），含 13 个 Swift 源文件、1 个 `MarkdownUI` 依赖、资源目录。
扫描方式：全文件通读 + 跨文件符号引用检索（grep）+ 工程配置（`project.pbxproj`）核对。

> 说明：工程使用 `PBXFileSystemSynchronizedRootGroup` 同步 `DeepMei/` 文件夹，因此该目录内所有 `.swift` 都会被编译；而仓库根目录的 `NFCReader.swift` 不在同步组内，不会被编译。

---

## 一、完全废弃 / 不再被任何模块引用的文件（死文件）

### 1. `NFCReader.swift`（仓库根目录，第 1–2 行，整文件仅 2 行）
- **依据**：文件内容本身就是说明——
  ```
  // 本文件已废弃，NFC 功能已移除。
  // 请在 Xcode 中手动删除此文件。
  ```
  且该文件位于 `DeepMei/` 同步组之外（`project.pbxproj` 的 `PBXFileSystemSynchronizedRootGroup` 只覆盖 `DeepMei/`），Xcode target 根本不会编译它。
- **结论**：应直接删除。

### 2. `DeepMei/Member.swift`（第 1–23 行，整文件）
- **定义**：`struct Member: Identifiable, Codable`。
- **依据**：全项目唯一引用 `Member` 的地方是 `MemberStore.swift`（`members: [Member]`、`[Member].self`）。而 `MemberStore` 本身也未被引用（见下一条）。无其他模块 `import` 或实例化 `Member`。
- **结论**：死数据模型，随 `MemberStore` 一起删除。

### 3. `DeepMei/MyDigitalMedia/MemberStore.swift`（第 1–79 行，整文件）
- **定义**：`class MemberStore`（含 `shared` 单例、`load()`、`find(id:)`、`find(name:)`）。
- **依据**：`MemberStore` 仅在自身文件中出现，全项目无任何文件调用 `MemberStore(...)` 或 `MemberStore.shared`。实际查询走 `MyRaspberryView` → `MemberService.shared.searchMember(...)`（飞书 API），并直接用 `RaspberryMember` 解析 `Member.json`，完全绕过了 `MemberStore`。
- **结论**：死模块，建议删除。

### 4. `DeepMei/Resources/QRScanner.swift`（第 1–68 行，整文件）
- **定义**：`class QRScanner: NSObject, ObservableObject`（基于 AVFoundation 的二维码扫描器，含 `startScanning`/`stopScanning`/`getPreviewLayer`）。
- **依据**：`QRScanner` 全项目仅在本文件中出现，无任何文件实例化或引用它。`DirectorCameraView` 用的是自己的 `CameraManager`，未接入此扫描器。
- **结论**：未接线的工具类（死代码）。若二维码扫描功能不再需要，应删除；若计划复用，需补上调用点。

### 5. `DeepMei/Person.swift`（第 1–73 行，整文件）
- **定义**：`struct Person` 和 `struct HallOfFameData`（含 `people` 静态数组，存放完整名人堂成员数据）。
- **依据**：`Person` 与 `HallOfFameData` 仅在自身文件中出现。名人堂页面 `HallOfFameView.swift` 自行定义了一套几乎相同的 `HallOfFameMember` 结构与 `allMembers` 数组（见第二条"重复数据"），并未使用 `Person.swift` 的模型。
- **结论**：死数据模块（其数据已被 `HallOfFameView` 复制）。建议删除，或在两处统一为单一数据源。

---

## 二、重复 / 冗余数据（死代码的变体）

### `HallOfFameView.swift` 第 505–541 行 `allMembers` 与 `Person.swift` 第 36–72 行 `HallOfFameData.people`
- **现象**：同一份"历届名人堂"成员数据被定义了两次，字段几乎一致（`id/name/term/role/tags/contribution/quote/grades/archive/photo`）。
- **依据**：`Person.swift` 那份已无任何引用（死代码）；`HallOfFameView` 这份是实际生效的副本。
- **风险**：两份数据若各自更新会产生"数据漂移"，名人堂展示与实际数据脱节。
- **建议**：删除 `Person.swift`，或重构为 `HallOfFameView` 复用 `Person` 模型（注意 `Person` 的 `termColor` 计算属性与 `HallOfFameMember` 重复，可一并合并）。

---

## 三、未被调用的工具函数 / 辅助方法 / 死属性

### 6. `DeepMei/MyDigitalMedia/MyRaspberryView.swift` 第 300–311 行 `loadMembers()`
- **现象**：私有方法，从 `Member.json` 解码到 `members` 属性。
- **依据**：全项目 grep `loadMembers` 仅命中定义处（第 300 行），从未被调用。实际搜索走 `performSearch()` → `MemberService.shared.searchMember(...)`。
- **结论**：死方法，可删除。

### 7. `DeepMei/MyDigitalMedia/MyRaspberryView.swift` 第 199 行 `members` 属性
- **现象**：`@State private var members: [RaspberryMember] = []`。
- **依据**：该属性仅被 `loadMembers()` 赋值，而 `loadMembers()` 从不调用，且 `body` 中只读取单个 `member` 而非 `members`。属性赋值后从未被读取。
- **结论**：死变量，随 `loadMembers()` 一并删除。

---

## 四、标记为 deprecated / obsolete 的代码

- **唯一命中**：`NFCReader.swift` 第 1 行——`// 本文件已废弃，NFC 功能已移除。`
- **其他**：全项目未发现 `@available(*, deprecated)`、`@available(*, unavailable)`、`obsolete`、`已废弃`（除 NFCReader 外）等标记。grep `deprecated|obsolete|已废弃|过时|unavailable|TODO|FIXME` 仅返回 NFCReader 与无关的 `ContentUnavailableView`（系统 API，非标记）。
- **结论**：除已废弃的 `NFCReader.swift` 外，无显式 deprecated/obsolete 标注的函数或变量。

---

## 五、注释掉的代码块

- **结论**：未发现。全项目无 `/* ... */` 块注释，也未发现以 `// func` / `// let` / `// var` / `// struct` / `// if` 等开头的"被注释掉的代码行"。现有 `//` 均为解释性注释或调试 `print`，属正常注释。
- 轻微冗余：`HallOfFameView.swift` 第 216–217 行连续两行 `// MARK: - 成员详情 Drawer/Sheet`（一行旧标题、一行新标题），属历史残留注释，非死代码，可顺手清理。

---

## 六、未被使用的依赖项

- **外部 Swift Package**：项目唯一依赖是 `MarkdownUI`（`swift-markdown-ui`，见 `project.pbxproj` 第 10、42、82、113、378 行）。
  - **依据**：`MarkdownArticleView.swift` 第 9 行 `import MarkdownUI`，第 24 行实际调用 `Markdown(...)` 渲染文章。**依赖处于活跃使用状态，无未使用依赖。**
- **内部框架 import**：各文件导入的 `SwiftUI / WebKit / AVFoundation / Photos / UIKit / CoreLocation / ImageIO / UniformTypeIdentifiers / Combine` 均被对应代码使用，无冗余 import。

---

## 七、附带观察（相关但非严格死代码）

1. **`DeepMei/Resources/Member.json`**：目前仅被死代码 `MemberStore.load()` 与未调用的 `MyRaspberryView.loadMembers()` 引用；运行时真实数据来自飞书 API。删除上述死代码后，该 JSON 将成为无引用的孤立资源，建议一并清理或显式用作本地兜底数据源。
2. **emoji 资源校验**：`ContentView.swift:70` 使用 `Int.random(in: 1...49)` 引用 `emoji-raspjam` 图集，目录实测含索引 1–49 共 50 个 imageset，**资源与代码范围一致，无误**。

---

## 汇总：建议清理清单

| 类别 | 文件 / 符号 | 行号范围 | 处理方式 |
|------|------------|----------|----------|
| 废弃文件（显式标注） | `NFCReader.swift` | 1–2 | 删除 |
| 死模块 | `Member.swift` | 1–23 | 删除 |
| 死模块 | `DeepMei/MyDigitalMedia/MemberStore.swift` | 1–79 | 删除 |
| 死工具类 | `DeepMei/Resources/QRScanner.swift` | 1–68 | 删除或补调用点 |
| 死数据模型 | `DeepMei/Person.swift` | 1–73 | 删除（数据已复制在 HallOfFameView） |
| 死方法 | `MyRaspberryView.swift` → `loadMembers()` | 300–311 | 删除 |
| 死属性 | `MyRaspberryView.swift` → `members` | 199 | 删除 |
| 重复数据 | `HallOfFameView.swift` → `allMembers` 与 `Person.swift` 重复 | 505–541 | 统一为单一数据源 |
| 孤立资源 | `DeepMei/Resources/Member.json` | — | 删除或设为本地兜底 |

> 清删前建议：由于 `MemberStore` / `loadMembers` / `Person` 均无任何调用方，直接删除不会影响现有功能（名人堂、我的树莓查询均走 `MemberService` 飞书接口）。删除后请用 Xcode 编译一次确认无遗漏引用。
